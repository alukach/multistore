{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Lambda API",
      "type": "lldb",
      "request": "attach",
      "program": "${workspaceFolder}/target/debug/lambda-api",
      "env": {
        "RUST_LOG": "s3s=debug"
      }
    },
  ]
}